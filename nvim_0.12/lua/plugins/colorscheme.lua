-- local ok, _ = pcall(require, "miasma")
-- if not ok then
--   vim.notify("Unable to load miasma")
--   return {}
-- end

vim.cmd("colorscheme miasma")
-- monokai.setup( { palette = require('monokai').ristretto })
--
-- local ok, monokai = pcall(require, "monokai")
-- if not ok then
--   vim.notify("Unable to load monokai")
--   return {}
-- end
--
-- monokai.setup( { palette = require('monokai').ristretto })
